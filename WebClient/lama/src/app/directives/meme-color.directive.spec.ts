import { MemeColorDirective } from './meme-color.directive';

describe('MemeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MemeColorDirective();
    expect(directive).toBeTruthy();
  });
});
