import { EqualIdDirective } from './equal-id.directive';

describe('EqualIdDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualIdDirective();
    expect(directive).toBeTruthy();
  });
});
