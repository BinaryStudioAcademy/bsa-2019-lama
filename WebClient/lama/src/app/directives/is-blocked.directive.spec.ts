import { IsBlockedDirective } from './is-blocked.directive';

describe('IsBlockedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsBlockedDirective();
    expect(directive).toBeTruthy();
  });
});
