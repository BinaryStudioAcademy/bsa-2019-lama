import { CheckFavoriteDirective } from './check-favorite.directive';

describe('CheckFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
